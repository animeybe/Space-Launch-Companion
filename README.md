# SLCP

Мобильное приложение для отслеживания предстоящих космических запусков и изучения ракет/площадок

**Особенности:**

- Пользователь видит ленту ближайших запусков
- Может открыть карточку запуска с
  подробностями (ракета, оператор, площадка, окно запуска)
- Отфильтровать по агентствам/странам/дате и тп
- Добавить запуск в «избранное»
- Работать офлайн (кеш в БД).

## Разделы

1. [Библиотеки](#libs)
2. [Скриншоты](#screenshots)

## 📦 Библиотеки <a id="libs"></a>

AndroidX Core KTX - расширения Kotlin для Android Framework (Упрощает работу с Android API через extension-функции)
Lifecycle Runtime KTX - управление жизненным циклом с корутинами (Для lifecycleScope в ViewModel и работы с жизненным циклом)
Activity Compose - интеграция Compose с Activity (Предоставляет setContent {} для настройки Compose UI)
Compose BOM (Bill of Materials) - управление версиями Compose (Гарантирует совместимость всех Compose-библиотек)
Room Database - абстракция над SQLite с Kotlin поддержкой (Для локального кэширования данных)
Retrofit - HTTP клиент для сетевых запросов (Для работы с SpaceDevs API)
Koin - Dependency Injection фреймворк (Упрощает управление зависимостями и внедрение в ViewModel)
Coil - библиотека для загрузки изображений (Для асинхронной загрузки и кэширования изображений запусков)
ViewModel Compose - интеграция ViewModel с Compose (Для viewModel() в Composable функциях)
Navigation Compose - навигация в Jetpack Compose (Для перехода между экранами (список → детали))
JUnit 4 - фреймворк для unit-тестирования (Основная библиотека для написания тестов (@Test, assertEquals и т.д.))
Kotlin Coroutines Test - тестирование корутин (Для runBlocking, TestDispatcher и управления временем в тестах)
MockK - библиотека для создания мок-объектов (ВРЕМЕННО ОТКЛЮЧЕНА из-за проблем с JDK) (Для создания заглушек зависимостей в тестах)
AndroidX Test - JUnit расширения для Android
Espresso - UI тестирование
Compose UI Test - тестирование Compose интерфейсов

## 🖼 Скриншоты <a id="screenshots"></a>

(ПОКА ПУСТО)
